<Window x:Class="Ejercicio_4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ejercicio_4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="15">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <Label Content="_Nombre:" 
                   Target="{Binding ElementName=txtNombre}"/>
            <TextBox Name="txtNombre" TabIndex="1"/>

            <Label Content="_Dirección:" Margin="0,10,0,0"
                   Target="{Binding ElementName=txtDireccion}"/>
            <TextBox Name="txtDireccion" TabIndex="3"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <Label Content="_Apellido:"
                   Target="{Binding ElementName=txtApellido}"/>
            <TextBox Name="txtApellido" TabIndex="2"/>

            <Label Content="_Código Postal:" Margin="0,10,0,0"
                   Target="{Binding ElementName=txtCodigoPostal}"/>
            <TextBox Name="txtCodigoPostal" TabIndex="4"/>
        </StackPanel>

    </Grid>
</Window>
