<Window x:Class="Ejercicio_5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ejercicio_5"
        mc:Ignorable="d"
        Title="Ejercicio 5 - ToolBar" Height="450" Width="800">

    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="ApplicationCommands.Cut" CommandTarget="{Binding ElementName=txtEditor}" ToolTip="Cortar">
                    <Image Source="./Assets/cortar.png" Width="16" Height="16" />
                </Button>

                <Button Command="ApplicationCommands.Copy" CommandTarget="{Binding ElementName=txtEditor}" ToolTip="Copiar">
                    <Image Source="./Assets/copiar.png" Width="16" Height="16" />
                </Button>

                <Button Command="ApplicationCommands.Paste" CommandTarget="{Binding ElementName=txtEditor}" ToolTip="Pegar">
                    <Image Source="./Assets/pegar.png" Width="16" Height="16" />
                </Button>

                <Separator />

                <Button Name="BtnMostrar" Click="BtnMostrar_Click" ToolTip="Mostrar texto seleccionado">
                    <Image Source="./Assets/info.png" Width="16" Height="16" />
                </Button>
            </ToolBar>
        </ToolBarTray>

        <TextBox Name="txtEditor" 
                 AcceptsReturn="True" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto">
        </TextBox>

    </DockPanel>
</Window>