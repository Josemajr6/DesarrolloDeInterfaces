<Window x:Class="Ejercicio_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ejercicio_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="15">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" 
               VerticalAlignment="Center"
               Target="{Binding ElementName=txtNombre}">
            <StackPanel Orientation="Horizontal">
                <Image Source="/Assets/usuario.png" Width="16" Height="16" Margin="0,0,5,0"/>

                <AccessText Text="_Nombre:" VerticalAlignment="Center"/>

            </StackPanel>
        </Label>

        <TextBox Name="txtNombre" Grid.Row="0" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

        <Label Grid.Row="1" Grid.Column="0" 
               VerticalAlignment="Center"
               Target="{Binding ElementName=txtEmail}">
            <StackPanel Orientation="Horizontal">
                <Image Source="Assets/email.png" Width="16" Height="16" Margin="0,0,5,0"/>

                <AccessText Text="_email:" VerticalAlignment="Center"/>

            </StackPanel>
        </Label>

        <TextBox Name="txtEmail" Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

        <TextBox Name="txtComentarios"
                 Grid.Row="2" Grid.Column="0" 
                 Grid.ColumnSpan="2" 
                 Margin="5,10,5,5"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"/>

    </Grid>
</Window>
